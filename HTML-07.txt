
								Lecture-61

Switch Selector
FAQ's:

5. How to define one set of statements for multiple cases?
A. By defining the cases line-by-line

	case 1:
	case 2:
	case 3:
		statements;
		break;

eg.

<script>
    function f1() {
        var product = prompt("Enter Product Name");
        switch (product) {
            case "TV":
            case "Mobile":
            case "Watch":
                document.write(`${product} belongs to Electronics category`);
                break;
            case "Casuals":
            case "Boots":
                document.write(`${product} belongs to Footwear category`);
                break;
            default:
                document.write(`Avail Products : TV, Mobile, Watch, Casuals, Boots only..`);
                break;
        }
    }
    f1()
</script>


- If you case is refering to one character then you can write multiple cases like for Y and N : Y,y,n,N etc
- but for string we use to lowercase()

eg.

<script>
    function f1() {
        var choice = prompt("Enter Choice Yes or No");
        switch (choice.toLowerCase()) {
            case "yes":
                document.write("You selected yes to continue");
                break;
            case "no":
                document.write("You selected No to stop");
                break;
            default:
                document.write("Please enter your choice Y or N");
                break;
        }
    }
    f1();
</script>

6. How you write case for range of values?
A. By using Boolean expression that verifies the range of values.
   IF case is using a Boolean expression then the switch must always use true as value.

Syntax:

	switch(true)
	{
		case booleanExpression:
			statements;
			break;
	}
	
- When we are writing cases with boolean value or expression , switch value must be true eg.switch(true)

eg.

<script>
    function f1() {
        var n = parseInt(prompt("Enter Number"));
        switch (true) {
            case (n >= 1 && n <= 10):
                document.write(`Your number ${n} is between 1 to 10`);
                break;
            case (n >= 11 && n <= 20):
                document.write(` Your number ${n} is between 11 to 20 `);
                break;
            default:
                document.write("Pleae enter a number between 1 to 20 only");
                break;
        }
    }
    f1();
</script>

7. Can we define case without jump statement?
A. Yes
	Yes. Switch will continue to execute next case until break occurs.
	If there is no break in next level cases then it executes up to end, which include the
	default.

- We can use this in offer prices like buy 1 get 1 free , buy 2 get 1 free

eg.

<script>
    function f1() {
        var n = parseInt(prompt("Enter Number"));
        switch (n) {
            case 1:
                document.write("You will get one<br>");
                break;
            case 2:
                document.write("You selected Two third one is free<br>");
            case 3:
                document.write("Collect your three<br>");
                break;
            default:
                document.write("Please enter a number between 1 to 3 only");
                break;
        }
    }
    f1();
</script>

eg.

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Switch Country</title>
    <link rel="stylesheet" href="../../node_modules/bootstrap/dist/css/bootstrap.css">
    <script>
        var regExp = / /;
        var tip = "";

        function SetValidation(pattern, toolTip, image) {
            regExp = pattern;
            tip = toolTip;
            document.getElementById("imgFlag").src = image;
            document.getElementById("txtMobile").placeholder = tip;
        }

        function CountryChange() {
            var countryName = document.getElementById("lstCountries").value;

            switch (countryName) {
                case "INDIA":
                    SetValidation(/^\+91\d{10}$/,
                        "+91 and 10 digits",
                        "../../public/image/INDIA.jpeg");
                    break;

                case "US":
                    SetValidation(/^\+\(1\)\(\d{3}\)\s\d{4}-\d{4}$/,
                        "+(1)(000) 0000-0000",
                        "../../public/image/US.jpeg");
                    break;

                case "UK":
                    SetValidation(/^\+\(44\)\(\d{3}\)\s\d{4}\s\d{4}$/,
                        "+(44)(000) 0000 0000",
                        "../../public/image/UK.jpeg");
                    break;

                default:
                    SetValidation(/.*/, "Please Select Country", "");
                    break;

            }
        }

        function VerifyClick() {
            var mobile = document.getElementById("txtMobile").value;
            var lblError = document.getElementById("lblError");

            if (mobile.match(regExp)) {
                document.write(`<h2>Your Mobile ${mobile} Verified Successfully ..</h2>`)
            } else {
                lblError.innerHTML = `Invalid Mobile: ${tip}`;
            }
        }

    </script>
</head>

<body class="container-fluid">
    <h2>Verify Your Mobile</h2>
    <dl class="w-25">
        <dt>Select Your Country</dt>
        <dd class="input-group">
            <select class="form-select" id="lstCountries" onchange="CountryChange()">
                <option>Choose Country</option>
                <option>INDIA</option>
                <option>US</option>
                <option>UK</option>
            </select>
            <img class="input-group-text" width="50" id="imgFlag">
        </dd>
        <dt>Mobile Number</dt>
        <dd><input type="text" class="form-control" id="txtMobile"></dd>
        <dd id="lblError" class="text-danger"></dd>
        <button class="btn btn-primary w-100 mt-3" onclick="VerifyClick()">Verify</button>
    </dl>
</body>

</html>

-----------------------------------------------------------------------------------------------------------

								Lecture-62

Number
String
Boolean Types
	- Boolean Operators
	- Conditional Statements
	if, else, switch, case, default

								Undefined Type
								
- Undefined is a value assigned to any reference if value is not defined.

Syntax:
	var x;
	document.write("x=" +x);	x = undefined

- You can use "undefined" keyword to verify value.

	if(x === undefined)			=> returns true if x is undefined, but not good.

	if(x)						=> returns true if x is defined, good code.
	
    if(x != undefined)			=> returns true if x is defined, not good code.

eg.

<script>
    var price;
    if (price) {
        document.write(`Price = ${price}`);
    } else {
        document.write("Please provide a value for price");

    }
</script>

- When you collect value at run time like prompt("Enter Value") and you not give any value then it return null

								Null
								
- It is a type defined for reference when value is not provided at runtime.
- You can verify by using "null" keyword.

Syntax:
	var price = prompt("Enter Price");
	if(price == null)
	{
	}

Summary
- Number
- String
- Boolean
- Null
- Undefined
- Symbol

								Non Primitive Data Types

- They are mutable types.
- They don't have fixed range for values.
- Value range varies according to memory available.
- They are stored in memory heap.
- Heap is memory which allows random access.
- JavaScript non-primitive includes
	a) Array
	b) Object
	c) Map
	d) Set (obsolete)

								Arrays

- If someone ask what is an array we dont directly say :
	it is collection of similar data type and store in continuous location 
- bcz in js we store multiple datatype value in array
	var values = [12,true,"Hello"]
- Instead we can say , in java array is collection of similar data type and store in continuous location 

- Computer Version
	ENIAC : invented by charless babegge
	EDSAC
	PDP : Data used to store in magnet taps
	PC
- Charles Babbage -> "father of the computer"

- Arrays are used to reduce overhead and complexity.
- Arrays can reduce overhead by storing values in sequential order.
- Arrays can reduce complexity by storing multiple values under the reference of one
  name.
- Array can handle various types of values in sequential order.
- Array size can change dynamically.

	Note: Several computing technologies can't handle different types of memory in
	sequential order and can't change the memory size dynamically. Hence they
	restrict array to similar data type and will not allow to change the size dynamically.
	[C, C++, Java, C#, ... ]

- JavaScript array size can change dynamically and can handle various types of values
  in sequential order.

Q. If i dont use array in my application then what problem i going to face ?
A. Application lead to overhead ie memory can't allocated in order then it fail in saving your data 
   It have memory but not use that memory, we sovlve that using defragment that memory 
   Here, we move used momory one side and unused memory another side,to avoid we use array

Configuring Array:
- Array declaration comprises of keyword and reference name.
- You can use var, let or const.

Syntax:
	var products;
	let categories;

- To allocate multiple memory you can initialize or assign memory using 2 techniques

		a) Array meta character "[ ]"
		b) Array Constructor "Array()"


Syntax:

	var products = [];				=> Initializing memory
	var products = new Array();

		(or)
	var products;
	products = [];					=> Assigning memory

	var products;
	products = new Array();

- In computer new means dynamic memory allocating operator
- Dynamic memory is good when we do discrite operation like ,means disconneted opration means open memeoty do operation distroy memory
- again he need do operation distroy memory
- Array meta character "[ ]" is used to allocate static memory, which defined memory for
  first request and continue the same across any number of requests.
- Array constructor is used to allocate a dynamic memory, which is a discreet memory.
  Memory is newly allocated for every request.

	Static	=> Continuous Memory
	Dynamic => Discreet Memory

-----------------------------------------------------------------------------------------------------------

								Lecture-63

Non Primitive
- Array
- [ ]
- Array()

- The main puropose of array is to reduct overhead and complexity
- Overhead is issue related to memory means memeory is available but application can't able to use.
- Array store value in sequential order so it reduct overhead.

Storing values in Array:
- You can initialize values into array memory.

Syntax :
	var values = [10, "A", true];
	var values = new Array(10, "A", true);

- You can assign values with reference of property.
- Property is a string type that maps to memory index, which is number type.

Syntax :
	var values = [ ];	// 0 is property
	values[0] = 10;		// 1 is property
	values["1"] = 20;	// valid you can re-assign, as var & let allows assignment.
	values[0] = 30;
	
- Array are not using index to acess or store it uses property which same as index number 
- Index number is int but property is string
	eg. var values = [];
		values[0] = 10;
		values[1] = "A";
		values["2"] = true;
		document.write(values); //10,A,true
eg.(proof)

<script>
    var values = [];
    values[0] = 10;
    values[1] = "A";
    values["2"] = true;
    // document.write(values);
    for (var property in values) {
        document.write(`${property}[${typeof property}] : ${values[property]} [${typeof values}] <br>`);
    }
</script>

- Array memory can handle any type of value
	a) Primitive type
	b) Non Primitive type
	c) Function

Syntax:
	var values = [ 10, "A", true, ["Delhi", "Hyd"], function(){ } ];

	values[0] = 10;
	values[1] = "A";
	values[2] = true;
	values[3] = ["Delhi",4"Hyd"]
	values[4] = function(){}

- Array can handle function in memory, but it must be anonymous.
- Anonymous function will not have a name.
- Anonymous functions are accessed by using IIFE pattern.
 [Immediately Invoked Function Expression]

Syntax:
	values[4]();
	values[3][0]; // Delhi

- A function can have name we can anywhere within scope we can invoke
- A function can't have name we can invoke immediately

Syntax: IIFE
	<script>
		(function()
		{
		document.write("Hello !");
		})();
	</script>

- var values = [[10, 20, 30], ["Delhi", "Hyd", ["TV", "Watch"]]];
  document.write(values[1][2][1]); //Watch

Ex:
	<script>
		var values = [[10, 20, 30], ["Delhi", "Hyd", ["TV", "Watch"]], function()
		{document.write("Function inside array")}];
		values[1][2][1] = "Fastrack Watch";
		document.write(values[1][2][1] +"<br>");
		values[2]();
	</script>

								Array Manipulations

1. Reading Array Elements

	toString()		: It returns all array elements with "," delimiter.
	join()			: It returns all array elements with custom delimiter.
	map()			: It is an iterator that reads elements in sequential order.
	forEach()		: It is an iterator that reads elements and index in sequential order

Ex:
	<script>
		var categories = ["Electronics", "Fashion", "Footwear"];
		document.write(categories.toString() +"<br>");
		document.write(categories.join("/"));
	</script>

- In modern programming we dont prefer loops bcz in js
- Generally loop required intializer(means where to start) ie. it requires a condition, you need a increment
- But iterator is software design pattern it read if there are value it stop if there are no value 
- Here we dont need to write intializer or condition and where to start where to stop 
- It will take care of that and iterator are more faster than loop.
- So, in js we mostly use iterator
- Iterator is read sequential elements.

Syntax: Map Iterator

	collection.map(function(value){
		// use value
	})

eg.
<script>
    var products = ["Mouse","keyboard","laptop","adaptor"];
    products.map(function(value){
        document.write(`<li>${value}</li>`);
    })
</script>


Syntax: forEach Iterator

	collection.forEach(function(value, index){
	// use index & value
	})

- By index we can delete item , if duplicate value are present we can manipulate according to index.

eg.

<script>
    var products = ["Mouse", "keyboard", "laptop", "adaptor"];
    products.forEach(function (value, index) {
        document.write(`<p>[${index}] ${value}</p>`);
    })
</script>

- You can use explicit iterators for reading array properties and elements.

	for .. in		It can read all properties of array.(properties means index number)
	for .. of		It can read all elements of array.
	
- in and of is operator

Syntax:

	for(var property in collection){

	}

	for(var value of collection){

	}


eg.

<script>
    var categories = ["Electronics", "Fashion", "Footwear"];
    for (var property in categories) {
        document.write(`<p>[${property}]: ${categories[property]} </p>`);
    }
</script>

- This is explicit iterator , so implicit iterator are faste bcz they are internally avl for an array

2. Filtering Elements

	find()		: It is an iterator that finds and returns only first occurrence value
				  that matches the given condition.

	filter()	: It is an iterator that returns all values that match given condition

	slice()		: It can returns the values between specified index.

Syntax:

	collection.find(function(value){		=> returns one value
		return value condition;
	});

	collection.filter(function(value){		=> returns an array [ ]
		return value condition;
	});

eg.

<script>
    var values = [34000, 44000, 65000, 57000, 85000];
    var result = values.find(function (val) {
        return val > 50000;
    });
    document.write(`find() -> ${result}<br>`);

    var resultArr = values.filter(function (val) {
        return val > 50000;
    })

    document.write(`filter() -> ${resultArr}<br>`);

    values.filter(function (val) {
        return val > 50000;
    }).map(function (val) {
        document.write(`filter() : map() -> ${val}<br>`);
    })
</script>

-----------------------------------------------------------------------------------------------------------

								Lecture-64

Configure Array
Store Elements
Read Array Elements

- Creating and Adding Elements into Page dynamically:

1. You can create any HTML element by using

	document.createElement()

Syntax:

	document.createElement("img")
	document.createElement("p")
	document.createElement("div")

- Difference between attribute and properties is attribute is not change where as properties is change
- here some property and attribute name are same like height,width,src but some are change like class - className

2. Assign created element to a memory reference

Syntax:
		var pic = document.createElement("img");	[HTMLElement type] here pic is html element

3. Every dynamic element requires "Properties"

Syntax:
		pic.width = "200";
		pic.height= "100";
		pic.src="./images/name.jpg";

4. Add element into page by using "appendChild()"

Syntax:

	<body> </body>
	<div id="container"> </div>

	document.querySelector("body").appendChild(pic);
	document.getElementByld("container").appendChild(pic);

eg. Image load

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dynamic Image Load</title>
    <script>
        function LoadClick() {
            var pic = document.createElement("img");

            pic.width = "100";
            pic.height = "100";
            pic.src = "../image/INDIA.jpeg";

            document.getElementById("container").appendChild(pic);
        }
    </script>
</head>

<body>
    <button onclick="LoadClick()">Load Image</button>
    <br><br>
    <div id="container">


    </div>
</body>

</html>

eg.	List load

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dynamic Image Load</title>
    <script>
        function LoadClick() {
            var ol = document.createElement("ol");

            ol.type = "a";

            var item1 = document.createElement("li");
            var item2 = document.createElement("li");

            item1.innerHTML = "Home";
            item2.innerHTML = "About";

            ol.appendChild(item1);
            ol.appendChild(item2);

            document.getElementById("container").appendChild(ol);
        }
    </script>
</head>

<body>
    <button onclick="LoadClick()">Load List</button>
    <br><br>
    <div id="container">


    </div>
</body>

</html>

- Below need to create with logic 

	<table width="25%" border="1">
        <thead>
            <tr>
                <th> Name </th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td> Samsung TV </td>
            </tr>
        </tbody>
    </table>

- In interview they ask to do above in js to check your logic 

eg.

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dynamic Element Load</title>
    <script>
        function LoadClick() {
            var table = document.createElement("table");
            table.width = "50%";
            table.border = "1";
             
            var thead = document.createElement("thead");
            var trHead = document.createElement("tr");
            var trBody = document.createElement("tr");
            var td = document.createElement("td");
            var th = document.createElement("th");
            var tBody = document.createElement("tbody");

            th.innerHTML = "Product Name";
            trHead.appendChild(th);
            thead.appendChild(trHead);
            table.appendChild(thead);

            td.innerHTML = "Samsung TV";
            td.style.textAlign = "center";
            
            trBody.appendChild(td);
            tBody.appendChild(trBody);
            table.appendChild(tBody);

            document.getElementById("container").appendChild(table);


        }
    </script>
</head>

<body>
    <button onclick="LoadClick()">Load Table</button>
    <br><br>
    <div id="container">

    </div>
</body>

</html>

eg.

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Array</title>
    <script>
        var menu = ["Home", "Shop", "Blog", "Pages", "Docs", "Contact"];
        function bodyLoad() {
            menu.map(function (item) {
                var li = document.createElement("li");
                li.innerHTML = item;
                document.querySelector("ol").appendChild(li);

                var button = document.createElement("button");
                button.type = "button";
                button.innerHTML = item;
                document.querySelector("nav").appendChild(button);

                var option = document.createElement("option");
                option.text = item;
                option.value = item;
                document.querySelector("select").appendChild(option);

                var ul_li = document.createElement("li");
                var input = document.createElement("input");
                input.type = "checkbox";
                input.value = item;

                var label = document.createElement("label");
                label.innerHTML = item;

                ul_li.appendChild(input);
                ul_li.appendChild(label);

                document.querySelector("ul").appendChild(ul_li);

            })
        }

    </script>
</head>

<body onload="bodyLoad()">
    <nav style="display: flex; justify-content: space-between; width: 50%;"> </nav>
    <ol> </ol>
    <select> </select>
    <ul style="list-style: none;">
        <!-- <li> <input type="checkbox"> <label> Home </label> </li> -->
    </ul>
</body>

</html>

- If we want control element individually we use above approach 
- If we want direct use as it is use below approach
- Simple tech to solve above 

	ul_li.innerHTML = `<input type="checkbox"> <label>${item}</label>`;

eg.

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Array</title>
    <script>
        var menu = ["Home", "Shop", "Blog", "Pages", "Docs", "Contact"];
        function bodyLoad() {
            menu.map(function (item) {
                var li = document.createElement("li");
                li.innerHTML = item;
                document.querySelector("ol").appendChild(li);

                var button = document.createElement("button");
                button.type = "button";
                button.innerHTML = item;
                document.querySelector("nav").appendChild(button);

                var option = document.createElement("option");
                option.text = item;
                option.value = item;
                document.querySelector("select").appendChild(option);

                var ul_li = document.createElement("li");

                ul_li.innerHTML = `<input type="checkbox"> <label>${item}</label>`;

                document.querySelector("ul").appendChild(ul_li);

            })
        }

    </script>
</head>

<body onload="bodyLoad()">
    <nav style="display: flex; justify-content: space-between; width: 50%;"> </nav>
    <ol> </ol>
    <select> </select>
    <ul style="list-style: none;">
        <!-- <li> <input type="checkbox"> <label> Home </label> </li> -->
    </ul>
</body>

</html>

- here we carate alert component of bootstap but there is no alert element in html
- so, we careate div and apply a class called alert

eg.

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Array</title>
    <link rel="stylesheet" href="../../node_modules/bootstrap/dist/css/bootstrap.css">
    <script src="../../node_modules/bootstrap/dist/js/bootstrap.bundle.js"></script>
    <script>
        var menu = ["Home", "Shop", "Blog", "Pages", "Docs"];
        function bodyLoad() {
            menu.map(function (item) {
                var div = document.createElement("div");
                div.className = "alert alert-dismissible alert-warning";
                div.innerHTML = `
                    <button class="btn btn-close" data-bs-dismiss="alert"></button>
                    <h3>${item}</h3>
                `;
                document.querySelector("section").appendChild(div);
            }
            )
        }
    </script>
</head>

<body onload="bodyLoad()" class="container-fluid">
    <section class="d-flex m-4 p-4 flex-wrap justify-content-between ">

    </section>
</body>

</html>

-----------------------------------------------------------------------------------------------------------

								Lecture-65

Adding Elements into Array:

	push()		: It adds new element(s) as last item.
	unshift()	: It adds new element(s) as first item.
	splice()	: It adds new element(s) at specific position.

Syntax:

	collection.push(e1, e2, ... )
	collection.unshift(e1, e2, ... )
	collection.splice(indexNumber, deleteCount, ...Items)

Note: Set delete count to "0" to add element without deleting existing.

Ex:

	menu.splice(1, 0, "About");
	menu.push("About, "Contact");

Removing Elements from Array:

	pop()		: It removes and returns last element.
	shift()		: It removes and returns first element.
	splice()	: It removes and returns specific element(s).

Syntax:

	collection.pop()
	collection.shift()
	collection.splice(1,2);		// from 1 index remove 2 items

Sorting Elements:

	sort()		: It sorts the elements and returns in ascending order.
	reverse()	: It arranges elements in reverse order.

Syntax:

	collection.sort()
	collection.reverse()
	
- Here if we want decending order then we sort() then reverse() 

eg.

	var menu = ["Delhi","Chennai", "Mumbai", "Goa", "Hyd", "Bangalore"];
	menu.sort();
	menu.reverse();

- Original order is not changed

- Element : Home , menu
- input : docs
- Element : Home, menu, Home, menu, docs

- To only add new element we need to use 
- document.getElementById("lstMenuItems").innerHTML = "";

- After adding above line 
- Element : Home , menu
- input : docs
- Element : Home , menu, docs

Eg.

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Array Manipulation</title>
    <link rel="stylesheet" href="../../node_modules/bootstrap-icons/font/bootstrap-icons.css">
    <link rel="stylesheet" href="../../node_modules/bootstrap/dist/css/bootstrap.css">
    <script src="../../node_modules/bootstrap/dist/js/bootstrap.bundle.js"></script>
    <script type="text/javascript">
        var menuItems = ["Home", "Shop"];

        function LoadMenuItems() {
            document.getElementById("lstMenuItems").innerHTML = "";
            menuItems.map(function (item) {
                var option = document.createElement("option");
                option.text = item;
                option.value = item;
                document.getElementById("lstMenuItems").appendChild(option);
            });
            document.getElementById("lblCount").innerHTML = menuItems.length;
        }

        function AddClick() {
            // var item_name = document.getElementById("txtName").value;
            // menuItems.push(item_name);
            // alert(`${item_name} Added Successfully.. `);
            // LoadMenuItems();
            // document.getElementById("txtName").value = "";
            // //After adding in inputbox we need to clean up

            var item_name = document.getElementById("txtName").value;
            if (menuItems.indexOf(item_name) == -1) {
                menuItems.push(item_name);
                alert(`${item_name} Added Successfully.. `);
                LoadMenuItems();
                document.getElementById("txtName").value = "";
            } else {
                alert(`${item_name} Exists`);
            }
        }


        function RemoveClick() {
            var selectedItem = document.getElementById("lstMenuItems").value;
            var selectedItemIndex = menuItems.indexOf(selectedItem);
            var flag = confirm(`Are you sure ?\nWant to delete ${selectedItem}`);
            if (flag == true) {
                menuItems.splice(selectedItemIndex, 1);
                LoadMenuItems();
            }
        }

        function RemoveAllclick() {
            var flag = confirm(`Are you sure ?\nWant to delete All item`);
            if (flag) {
                menuItems.length = 0;
                LoadMenuItems();
            }
        }

        function EditClick() {
            var selectedItem = document.getElementById("lstMenuItems").value;
            document.getElementById("txtEdit").value = selectedItem;
        }

        function SaveClick() {
            var newValue = document.getElementById("txtEdit").value;
            var selectedItem = document.getElementById("lstMenuItems").value;
            var selectedIndex = menuItems.indexOf(selectedItem);

            menuItems[selectedIndex] = newValue;
            LoadMenuItems();
        } 

        function SortAsc() {
            menuItems.sort();
            LoadMenuItems();
        }
        function SortDesc() {
            menuItems.sort();
            menuItems.reverse();
            LoadMenuItems();

        }

    </script>
</head>

<body class="container-fluid" onload="LoadMenuItems()">

    <div class="mt-3 w-50">
        <h2>Design Your menu</h2>
        <div>
            <label class="form-label fw-bold">Add menu items</label>
            <div class="input-group">
                <input type="text" class="form-control" id="txtName" placeholder="New menu item name">
                <button class="btn btn-primary" onclick="AddClick()">Add item</button>
            </div>
        </div>

        <div class="my-2">
            <label class="form-label fw-bold">Menu Items <span class="badge bg-danger" id="lblCount"></span> </label>
            <button onclick="SortAsc()" class="bi bi-sort-alpha-down btn btn-success"></button>
            <button onclick="SortDesc()" class="bi bi-sort-alpha-up btn btn-warning"></button>
            <div class="my-3">
                <select class="form-select" size="3" id="lstMenuItems">
                </select>
                <div class="mt-2 row">
                    <div class="col">
                        <button data-bs-toggle="modal" onclick="EditClick()" data-bs-target="#edit"
                            class="btn w-100 btn-warning bi bi-pen-fill">Edit</button>
                        <div class="modal fade" id="edit">
                            <div class="modal-dialog">
                                <div class="modal-content">
                                    <div class="modal-header">
                                        <h3>Edit Item</h3>
                                    </div>
                                    <div class="modal-body">
                                        <input type="text" class="form-control" id="txtEdit">
                                    </div>
                                    <div class="modal-footer">
                                        <button class="btn btn-success" data-bs-dismiss="modal"
                                            onclick="SaveClick()">Save</button>
                                        <button class="btn btn-danger" data-bs-dismiss="modal">Cancel</button>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="col">
                        <button onclick="RemoveClick()" class="btn w-100 btn-danger bi bi-trash-fill">Remove</button>
                    </div>
                    <div class="col">
                        <button onclick="RemoveAllclick()" class="btn w-100 btn-outline-danger bi bi-trash">Remove
                            All</button>
                    </div>
                </div>
            </div>
        </div>
    </div>

</body>

</html>

-----------------------------------------------------------------------------------------------------------

								Lecture-66

Array with Spread Operator:
- You can use " ... " which is a spread operator to copy the elements of an array into another.
- It allows to spread the values of a collection into individual references.

Syntax:
	var one = ["A", "B"];
	var two = [one];					// two have 1 element ie ["A", "B"]
	var two = [one, "C", "D"];			// two have 3 elements
	var two = [...one, "C", "D"];		// two have 4 elements								

Syntax:
	var one = ["A", "B"];
	var two = ["C", "D"];
	var three = [ ...one, two];			// 3 elements
	var three = [ ...one, ...two];		// 4 elements
	var three = [one, two];				// 2 elements

- Array functions for finding elements

	indexOf()
	lastlndexOf()
	find()
	filter()


								Object Type
								
- Object is used to keep all related data and logic together.
- "Alan kay" introduced the concept of object into computer programming in early 1960's.
- Technically object is a key-value pair.
- Key is always string type and value can be any type.

Syntax:
	var obj = {
		"Key": value
		"Key": value,
	}

- Value can be
	a) Primitive
	b) Non Primitive
	c) Function

Syntax:
	var obj = {
		"Key": number,
		"Key": string,
		"Key": new Array(),
		"Key": { object },
		"Key": function(){}
	}

- Now in Programming Data is maintained in two format ie xml and json
- google map is designed with xml
- Now a day we use json more than xml
- Before xml and json we take data in COM to marshal

- If object comprises of only data without any action (function) defined, then it is referred as "JSON".
- JSON is JavaScript Object Notation, It is a format for data.
	* JSON is light weight
	* JSON is faster in transporting and rendering
	* JSON doesn't require COM to Marshal [Converting object to binary & vice versa]
	 Before JSON became common, data was converted from an object or database format into a transferable format
	 (serialization/marshalling) and sent over the network.
	 At the receiving side, the data was converted back into the required object format (deserialization/unmarshalling).
	 Marshalling is the process of converting an object into a transferable format (such as JSON or XML), 
	 and unmarshalling is converting it back into an object.
	 
	* JSON can flow through firewalls.
	* JSON is not infected by VIRUS.

Syntax:
	{
		"Name": "TV",
		"Price": 34000.44,
		"Stock": true,
		"ShippedTo": ["Delhi", "Hyd"],
		"Rating": { "Rate": 4.6, "Count": 4500}
	}

- Object values are accessed with reference of "Key".

Syntax:
	var product = { "Name": "TV", "Price":56000 };
	
	product.Name;		// returns TV
	product.Price;		// returns 56000
	
- You can assign new values if declared with "var or let".

	product.Name = newname;
	product.Price = newprice;	

eg.

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>JSON PRESENTATION</title>
    <link rel="stylesheet" href="../../node_modules/bootstrap-icons/font/bootstrap-icons.css">
    <link rel="stylesheet" href="../../node_modules/bootstrap/dist/css/bootstrap.css">
    <script>
        var mobile = {
            "title": "Apple iPhone 15 (Black, 128 GB)",
            "price": 64999,
            "rating": { "rate": 4.6, "ratings": 39755, "reviews": 2187 },
            "offers": [
                "Bank Offer Get ₹50 Instant Discount on first Flipkart UPI transaction on order of ₹200 and above T & C",
                "Bank Offer 5 % Cashback on Flipkart Axis Bank Card T & C",
                "Bank Offer ₹1000 off on Selected Bank Net Banking Transaction T & C",
                "Special Price Get extra ₹12901 off(price inclusive of cashback / coupon) T & C"
            ],
            "image": "../../public/image/iphone-black.jpg"
        }

        function bodyLoad() {
            document.getElementById("imgProduct").src = mobile.image;
            document.getElementById("lblTitle").innerHTML = mobile.title;
            document.getElementById("lblRating").innerHTML =
                `${mobile.rating.rate} <span class="bi bi-star-fill"></span>`;
            document.getElementById("lblReviews").innerHTML = `<b> ${mobile.rating.ratings.toLocaleString()} rating ${mobile.rating.reviews.toLocaleString()} reviews</b>`;
            document.getElementById("lblPrice").innerHTML = mobile.price.toLocaleString("en-in", { style: "currency", currency: "INR" })

            mobile.offers.map(function (offer) {
                var li = document.createElement("li");
                li.className = "text-success bi bi-tag-fill my-2";
                li.innerHTML = `<span class="text-secondary"> ${offer}</span>`;
                document.querySelector("ul").appendChild(li);

            });
        }
    </script>
</head>

<body class="container-fluid" onload="bodyLoad()">
    <div class="mt-4 row">
        <div class="col-3">
            <img id="imgProduct" width="100%">
        </div>
        <div class="col-9">
            <div id="lblTitle" class="h5"> </div>
            <div class="my-3">
                <span class="bg-success p-1 rounded text-white" id="lblRating"></span>
                <span id="lblRating"></span>
                <span id="lblReviews" class="text-secondary ms-3"></span>
            </div>
            <div class="my-4">
                <div id="lblPrice" class="h3"></div>
            </div>

            <div class="my-3">
                <ul class="list-unstyled">
                </ul>
            </div>

        </div>
    </div>
</body>

</html>

-----------------------------------------------------------------------------------------------------------

								Lecture-67

Object Data Type
JSON - JavaScript Object Notation

Note:
	- You can store data offline in a JSON file. So that you can access from any page.
	- JSON file must have the extension ".json"
	- To access data from JSON file you need "Ajax Requests".

- Here we deal with data form external file 
- Create new folder as data and here we kept our data related file
- create file product.json
- paste yesterday product data 
- Synchronous means one after another.
- Asynchronous means when work is going on allow other work to do in background without blocking tasks.

							JavaScript Ajax

- Ajax is Asynchronous JavaScript And XML.
- By using ajax without loading complete page , it bring new data in page - it improve performance of application
- Ajax used to enable partial postback 
- Partila postback means without posting entire page, only specific portion of page can be posted
- Ajax :
 Asynchronous -  Perform action without reloading complete page
 that action are defined through a language called javascript 
 and xml is medium used to handle communation. 
- Asynchronous allows to handle a task without blocking other tasks in process.
- Ajax enables "Partial Postback". It can post specific portion of page without submitting
  entire page.
- Ajax allows to add new content into page without reloading the complete page.
- JavaScript is used as browser language to hand Async request.
- XML is used as medium to transport data in async request.
- In a browser the Ajax calls are managed by "XMLHttpRequest" object. 

- href means set a new url
- XMLHttpRequest object it can GET,POST,PUT,DELETE data from server

GET : fetch data form server
POST : Submit data to server
PUT : Edit/Modify data on server and save the chnages 
DELETE : Remove the data form server.

Step-1: Create a new XMLHttpRequest object

	var http = new XMLHttpRequest();

Step-2: Configure the request by using "open()" method

	http.open("requestMethod", "url", async:boolean)

	requestMethod	=> GET, POST, PUT, PATCH, DELETE 
	Async			=> It is default false.

Step-3: Send the request for processing

	http.send();

- This request will procssed by browser.(bcz this is browser window object.)
- Other browser window object are : alert(), conform()

- Every ajax request have 4 phases : initial phase, success phase, complete phase, ready phase - (here response is ready) 

Setp-4: You have to check the readyState "onreadystatechange" function.

	http.onreadystatechange = function() {

		if(http.readyState == 4){
			//on 4th stage response is ready we can get the response and use the reponse
		}

	}


	readyState == 1			=> Initial
	readyState == 2			=> Success
	readyState == 3			=> Complete
	readyState == 4			=> Response Ready


Step-5: Collect the response details using http object

	http.responseText
	http.responseXML

Ex:
1. Create a new folder "data"
2. Add a new file "help.txt" with some content inside
3. Add HTML page
	ajax-demo.html

eg.

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ajax Demo</title>
    <script>
        function bodyload() {
            var now = new Date();
            console.log(`Page Loaded on : ${now.toLocaleTimeString()}`);
        }

        function LoadClick() {
            var now = new Date();
            console.log(`Load button Clicked at ${now.toLocaleTimeString()}`);
            // here if page load time != load button click then our page not loaded when click on button 
            
            var http = new XMLHttpRequest();
            http.open("get", "../../data/help.txt", true);
            http.send();

            http.onreadystatechange = function () {
                if (http.readyState == 4) {
                    document.getElementById("container").innerHTML = http.responseText;
                }
            }
        }
    </script>
</head>

<body onload="bodyload()">
    <button onclick="LoadClick()">Load Help</button>
    <br><br>
    <pre id="container">

    </pre>
</body>

</html>

- Note: When we read json file, You have to convert text into JSON by using "JSON.parse()"

1. data/product.json

{
    "title": "Apple iPhone 15 (Black, 128 GB)",
    "price": 64999,
    "rating": {
        "rate": 4.6,
        "ratings": 39755,
        "reviews": 2187
    },
    "offers": [
        "Bank Offer Get ₹50 Instant Discount on first Flipkart UPI transaction on order of ₹200 and above T & C",
        "Bank Offer 5 % Cashback on Flipkart Axis Bank Card T & C",
        "Bank Offer ₹1000 off on Selected Bank Net Banking Transaction T & C",
        "Special Price Get extra ₹12901 off(price inclusive of cashback / coupon) T & C"
    ],
    "image": "../../public/image/iphone-black.jpg"
}

2. Ajax-demo.html

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ajax Demo</title>
    <link rel="stylesheet" href="../../node_modules/bootstrap/dist/css/bootstrap.css">
    <link rel="stylesheet" href="../../node_modules/bootstrap-icons/font/bootstrap-icons.css">
    <script>
        function bodyLoad() {
            var product = {};
            var http = new XMLHttpRequest();
            http.open("get", "../../data/product.json", true);
            http.send();

            http.onreadystatechange = function () {
                if (http.readyState == 4) {
                    product = JSON.parse(http.responseText);
                    document.getElementById("imgProduct").src = product.image;
                    document.getElementById("lblTitle").innerHTML = product.title;
                    document.getElementById("lblRating").innerHTML = product.rating.rate;
                    document.getElementById("lblReviews").innerHTML =
							`${product.rating.ratings.toLocaleString()} ratings ${product.rating.reviews.toLocaleString()} reviews `;
                    document.getElementById("lblPrice").innerHTML = "$" + product.price.toLocaleString();
                    product.offers.map(function (offer) {
                        var li = document.createElement("li");
                        li.className = "bi bi-tag-fill text-success";
                        li.innerHTML = `<span class="text-secondary"> ${offer} </span>`;
                        document.getElementById("lstoffers").appendChild(li);
                    });
                }
            }
        }
    </script>
</head>

<body onload="bodyLoad()" class="container-fluid">
    <div class="row mt-3">
        <div class="col-3">
            <img width="100%" id="imgProduct">
        </div>
        <div class="col-9">
            <div class="h4" id="lblTitle"></div>
            <div class="my-3">
                <span class="bg-success rounded badge p-2">
                    <span id="lblRating"></span>
                    <span class="bi bi-star-fill text-white"></span>
                </span>
                <span class="fw-bold text-secondary" id="lblReviews"></span>
            </div>
            <div class="my-3">
                <div class="h3" id="lblPrice"></div>
            </div>
            <div class="my-3">
                <ul class="list-unstyled" id="lstoffers">

                </ul>
            </div>
        </div>
    </div>
</body>

</html>
</html>

-----------------------------------------------------------------------------------------------------------

								Lecture-68

XMLHttpRequest Issues
- It is not async by default.
- You have to explicitly configure async.
- It requires various parsing techniques to convert response data.
	Response Data have 2 formats 1)text 2)xml
- It is not good in handling errors.


						JavaScript "fetch()" Promise

- Promise is by default async.
- It provides good error handling methods.
- It returns every response in binary format.
- However it requires conversion of data.

Syntax:
	fetch("url")
		.then(function(){ on success })
		.catch(function(){ on failure })
		.finally(function(){ always })

- then() function returns a response from URL, which is in "binary format".
- You need another then() function that converts binary to required format.

Syntax:

	.then(function(response){
		return response.json();
	}
	.then(function(data){
		// use data
	})

- fetch() belong to window object

eg.

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ajax Demo</title>
    <link rel="stylesheet" href="../../node_modules/bootstrap/dist/css/bootstrap.css">
    <link rel="stylesheet" href="../../node_modules/bootstrap-icons/font/bootstrap-icons.css">
    <script>
        function bodyLoad() {
            fetch("../../data/product.json")
                .then(function (response) {
                    return response.json();
                })
                .then(function (product) {
                    document.getElementById("imgProduct").src = product.image;
                    document.getElementById("lblTitle").innerHTML = product.title;
                    document.getElementById("lblRating").innerHTML = product.rating.rate;
                    document.getElementById("lblReviews").innerHTML = `${product.rating.ratings.toLocaleString()} ratings ${product.rating.reviews.toLocaleString()} reviews `;
                    document.getElementById("lblPrice").innerHTML = "$" + product.price.toLocaleString();
                    product.offers.map(function (offer) {
                        var li = document.createElement("li");
                        li.className = "bi bi-tag-fill text-success";
                        li.innerHTML = `<span class="text-secondary"> ${offer} </span>`;
                        document.getElementById("lstoffers").appendChild(li);
                    });
                })
                .catch(function (err) {
                    console.log(err);
                })
                .finally(function () {
                    console.log(`Ajax request Completed`);
                })
        }
    </script>
</head>

<body onload="bodyLoad()" class="container-fluid">
    <div class="row mt-3">
        <div class="col-3">
            <img width="100%" id="imgProduct">
        </div>
        <div class="col-9">
            <div class="h4" id="lblTitle"></div>
            <div class="my-3">
                <span class="bg-success rounded badge p-2">
                    <span id="lblRating"></span>
                    <span class="bi bi-star-fill text-white"></span>
                </span>
                <span class="fw-bold text-secondary" id="lblReviews"></span>
            </div>
            <div class="my-3">
                <div class="h3" id="lblPrice"></div>
            </div>
            <div class="my-3">
                <ul class="list-unstyled" id="lstoffers">

                </ul>
            </div>
        </div>
    </div>
</body>

</html>

							Object with functions	
							
- Object comprises of data and functionality.
- Data is defined as property and action is defined as function.
- The members of object are accessed outside object using object reference name and
  a key name.
- The members of object are accessed within object using "this" keyword.
- properties describe how object look like , ie height,width,color
- Object what can do called methods
- Button is an object, button height,width are properties
- when we click button it will close/open : open/close are action ie methods

Syntax:
	{
		Key : value,
		Key : function(){ }
	}

eg.

var product = {
	Name: "Samsung TV",
	Price: 34000.44,
	Qty: 2,
	Total : function(){
		return this.Qty * this.Price;
	},
	Print: function(){
		document.write(`Name=${this.Name}<br>Price=${this.Price}<br>`);
	}
}

- here if you keep data empty we use any number of data

var product = {
	Name: "",
	Price: 0.0,
	Qty: 0,
	Total : function(){
		return this.Qty * this.Price;
	},
	Print: function(){
		document.write(`Name=${this.Name}<br>Price=${this.Price}<br> Total = ${this.Total}`);
	}
}

product. Name = "Samsung TV";
product. Price = parseInt(prompt("Enter Price"));
product. Qty = parseInt(prompt("Enter Quantity"));
product.Print();


- [] : Array
- {} : Object
- [{},{}] : Array of Objects

Eg. Array of Object

- data/products.json

[
    {
        "title": "Apple iPhone 15 (Black, 128 GB)",
        "price": 64999,
        "rating": {
            "rate": 4.6,
            "ratings": 39755,
            "reviews": 2187
        },
        "offers": [
            "Bank Offer Get ₹50 Instant Discount on first Flipkart UPI transaction on order of ₹200 and above T & C",
            "Bank Offer 5 % Cashback on Flipkart Axis Bank Card T & C",
            "Bank Offer ₹1000 off on Selected Bank Net Banking Transaction T & C",
            "Special Price Get extra ₹12901 off(price inclusive of cashback / coupon) T & C"
        ],
        "image": "../../public/image/iphone-black-crop.jpg"
    },
    {
        "title": "Apple iPhone 17 (Green, 1 TB)",
        "price": 199999,
        "rating": {
            "rate": 4.5,
            "ratings": 29355,
            "reviews": 1187
        },
        "offers": [
            "Bank Offer Get ₹50 Instant Discount on first Flipkart UPI transaction on order of ₹200 and above T & C",
            "Bank Offer 5 % Cashback on Flipkart Axis Bank Card T & C",
            "Bank Offer ₹1000 off on Selected Bank Net Banking Transaction T & C",
            "Special Price Get extra ₹12901 off(price inclusive of cashback / coupon) T & C"
        ],
        "image": "../../public/image/iphone-green-crop.jpg"
    },
    {
        "title": "Apple iPhone 17 (Pink, 5 TB)",
        "price": 201899,
        "rating": {
            "rate": 4.7,
            "ratings": 88755,
            "reviews": 2487
        },
        "offers": [
            "Bank Offer Get ₹50 Instant Discount on first Flipkart UPI transaction on order of ₹200 and above T & C",
            "Bank Offer 5 % Cashback on Flipkart Axis Bank Card T & C",
            "Bank Offer ₹1000 off on Selected Bank Net Banking Transaction T & C",
            "Special Price Get extra ₹12901 off(price inclusive of cashback / coupon) T & C"
        ],
        "image": "../../public/image/iphone-pink-crop.jpg"
    }
]

- ajax-demo.html

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Products Representation</title>
    <link rel="stylesheet" href="../../node_modules/bootstrap/dist/css/bootstrap.css">
    <link rel="stylesheet" href="../../node_modules/bootstrap-icons/font/bootstrap-icons.css">
    <script>
        function bodyLoad() {
            fetch("../../data/products.json")
                .then(function (response) {
                    return response.json();
                })
                .then(function (products) {
                    products.map(function (product) {
                        var div = document.createElement("div");
                        div.className = "row my-4 border border-1 m-4 p-2";
                        div.innerHTML = `
                            <div class="col-2">
                                <img src="${product.image}" width="100%">
                            </div>
                            <div class="col-8">
                                <div class="h4">${product.title}</div>
                                <div class="my-2">
                                    <span class="badge bg-success p-1 rounded">
                                        ${product.rating.rate} 
                                            <span class="bi bi-star-fill"> </span>    
                                    </span>
                                    <span class="ms-3 text-secondary fw-bold">
                                        ${product.rating.ratings} ratings &
                                        ${product.rating.reviews} reviews
                                    </span>
                                </div>
                            </div>
                            <div class="col-2">
                                <div class="h2"> &#8377 ${product.price.toLocaleString()}</div>
                            </div>
                        `;

                        document.querySelector("section").appendChild(div);
                    })
                })
        }
    </script>
</head>

<body class="container-fluid" onload="bodyLoad()">
    <section>

    </section>
</body>

</html>


----------------------------------------  HW : NASA API'S https://api.nasa.gov/ , repesent it -----------------------------

eg.

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Store API</title>
    <link rel="stylesheet" href="../../node_modules/bootstrap/dist/css/bootstrap.css">
    <link rel="stylesheet" href="../../node_modules/bootstrap-icons/font/bootstrap-icons.css">
    <script>
        function bodyLoad() {
            fetch("https://fakestoreapi.com/products")
                .then(function (response) {
                    return response.json();
                })
                .then(function (products) {
                    products.map(function (product) {
                        var div = document.createElement("div");
                        div.className = "card m-2 p-2";
                        div.style.width = "200px";
                        div.innerHTML = `
                            <a href="${product.image}" target="_blank"> <img class="card-img-top" src=${product.image} height="200"> </a>
                            <div class="card-header">
                                ${product.title.substring(0,10)}
                            </div>
							
                            <div class="card-body col border border-1" >
							
								<div class="row-4">    
									<b >Desc. : </b> ${product.description.substring(0,50)}<br>
								</div>
								<div class="row-4">    
									<b >Price : </b> ${product.price}<br>
								</div>
								<div class="row-4">    
									 <b >Category : </b> ${product.category}
								</div>
																   
                            </div>
							
                            <div class="card-footer bg-success text-white align-content-center">
                                ${product.rating.rate}
                                <span class="bi bi-star-fill"></span> &nbsp&nbsp
                                ${product.rating.count}
                                <span class="bi bi-pencil-fill"></span>
                            </div>
                        `;

                        document.querySelector("section").appendChild(div);
                    })
                })
        }
    </script>
</head>

<body onload="bodyLoad()" class="container-fluid">

    <section class="d-flex flex-wrap">

    </section>
</body>

</html>

-----------------------------------------------------------------------------------------------------------

								Lecture-69

Object Manipulations:

1. How to access all keys from object?
A.
	a) for .. in
	b) Object.keys() : keys = properties

Syntax:
	for (var key in object)
	{
	}

	Object.keys(objectName).map()

- Object.keys(objectName) : it return array of keys

eg.

<script>
    var product = {
        id: 1,
        title: "Samsung TV",
        price: 45000.44,
        stock: true
    }
    for (var key in product) {
        document.write(`${key} ${product[key]} <br>`);
    }
    Object.keys(product).map(function (key) {
        document.write(`<li>${key} : ${product[key]}</li>`);

    });
</script>

2. How to get the count of keys in object?
A.
	Object.keys(product).length
Ex:
	document.write(`Total Count of Keys : ${Object.keys(product).length}`);

3. How to delete a key?
A. By using JS "delete" operator

Syntax:
	delete object.key;
	delete product.price;


4. How to find a key?
A. By using "in" operator

Syntax:
	"key" in objectName;	// true if object have the given key

eg.

<script>
    var product = {
        id: 1,
        title: "Samsung TV",
        price: 45000.44,
        stock: true,
        rating: 4.5
    }
    if("rating" in product){
        document.write(`Product Rating is ${product.rating}`)
    }else{
        document.write("Rating Not Avalable.....")
    }
</script>

- Symbol() : symbol type are used in square press []
- It will hidden we can show by using objectName[symbolWhichHidden]

eg

5. How to hide a key in iterations?
A. By using JS "Symbol" data type.

Syntax:
	var id = Symbol();

	var product = {
		[id] : 1,		//here we need to write in []
		title: "TV"
	}
	Object.keys(product).map();		// id is ignored
	
	document.write(product[id]);
	
eg.

<script>

    var id = Symbol();

    var product = {
        [id]: 1,
        title: "Samsung TV",
        price: 45000.44,
        stock: true,
        rating: 4.5
    }
    
    Object.keys(product).map(function(Key){
        document.write(`<li>${Key} </li>`)
    })

    document.write(`Product id : ${product[id]}`);
</script>

- Note: In HTML form you can handle hidden fields by using input type "hidden".

Syntax:
	<input type="hidden" name="Userld" value="john_nit">

- it not show in form but when we submit the form it submit with form data
- In below eg we see in url 
  like http://127.0.0.1:5500/javascript-examples/object.html?UserId=john_123&UserName=john&age=22

eg.

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <form>
        <dl>
            <dd><input type="hidden" name="UserId" value="john_123"></dd>
            <dt>Username</dt>
            <dd><input type="text" name="UserName" value="john"> </dd>
            <dt>Age</dt>
            <dd><input type="number" name="age" value="22"> </dd>
        </dl>
        <button>Submit</button>
    </form>
</body>

</html>

6. How to know the data type of a value stored in key?
A. By using "typeof" operator.

Syntax:

	typeof		 object.key		// returns data type
	typeof		 object[key]
	typeof		 product[price]

eg.

<script>
    var product = {
        id: 1,
        title: "Samsung TV",
        price: 45000.44,
        stock: true,
        rating: {
            rate: 5,
            reviews: 2541
        }
    }
    document.write(`Type of : ${typeof product.rating}`);
</script>

eg. Nested Map {Need to practice below example}

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Object Demo</title>
    <script>
        var menu = [
            { Category: "Electronic", Products: ["TV", "MOBILE", "WATCH"] },
            { Category: "Fshion", Products: ["KIDS", "MEN", "WOMEN"] }
        ]

        function bodyLoad() {
            menu.map(function (item) {
                var ol_li = document.createElement("li");
                ol_li.innerHTML = item.Category;
                item.Products.map(function (product) {
                    var ul = document.createElement("ul");
                    var ul_li = document.createElement("li");
                    ul_li.innerHTML = product;
                    ul.appendChild(ul_li);
                    ol_li.appendChild(ul);
                    document.querySelector("ol").appendChild(ol_li)
                })
            })
        }
    </script>
</head>

<body onload="bodyLoad()">
    <ol>

    </ol>
</body>

</html>

eg.	Dropdown with optGroup

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Object Demo</title>
    <script>
        var menu = [
            { Category: "Electronic", Products: ["TV", "MOBILE", "WATCH"] },
            { Category: "Fashion", Products: ["KIDS", "MEN", "WOMEN"] }
        ]

        function bodyLoad() {
            menu.map(function (item) {
                var optGroup = document.createElement("optgroup");
                optGroup.label = item.Category;

                item.Products.map(function (product) {
                    var option = document.createElement("option");
                    option.value = product;
                    option.text = product;

                    optGroup.appendChild(option);
                    document.querySelector("select").appendChild(optGroup)
                })

            })
        }
    </script>
</head>

<body onload="bodyLoad()">
    <select></select>
</body>

</html>


eg.

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Object Demo</title>
    <script>
        var topic = [
            { title: "HTML", description: "This is used to create structure" },
            { title: "CSS", description: "This is used to style html element" }
        ]
        function bodyLoad() {
            topic.map(function (item) {
                var dt = document.createElement("dt");
                var dd = document.createElement("dd");
                console.log(item);

                dt.innerHTML = item.title;
                dd.innerHTML = item.description;
                var dl = document.querySelector("dl");
                dl.appendChild(dt);
                dl.appendChild(dd);
            })
        }
    </script>
</head>

<body onload="bodyLoad()">
    <dl></dl>
</body>

</html>

eg.

<script>

    var products = [
        { Name: "Nike Causals", Category: "Footwear" },
        { Name: "Samsung TV", Category: "Electronics" },
        { Name: "Woodland Boots", Category: "Footwear" },
        { Name: "Jeans", Category: "Fashion" },
    ]
    products.filter(function (product) {
        return product.Category == "Footwear" || product.Category == "Electronics";
    }).map(function (product) {
        document.write(product.Name + "<br>");
    })
</script>


eg.

<script>

    var products = [
        { Name: "Nike Causals", Category: "Footwear", price: 3400 },
        { Name: "Samsung TV", Category: "Electronics", price: 5200 },
        { Name: "Woodland Boots", Category: "Footwear", price: 5180 },
        { Name: "Jeans", Category: "Fashion", price: 8180 },
    ]

    var total = 0;

    products.filter(function (product) {
        return product.Category == "Footwear";
    }).map(function (product) {
        total += product.price;
        document.write(`${product.Name} :  ${product.price} <br>`);
    })

    document.write(`Total Price : ${total}`);
</script>























